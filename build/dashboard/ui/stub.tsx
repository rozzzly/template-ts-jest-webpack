import * as React from 'react';

export const Box: React.SFC<any> = (props) => React.createElement('box', props);
export const Element: React.SFC<any> = (props) => React.createElement('element', props);
export const Text: React.SFC<any> = (props) => React.createElement('text', props);
